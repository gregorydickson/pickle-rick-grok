# Pickle Rick Grok — AGENTS.md

Local rules for agents working in this source tree.

## Project Objectives

Build a system that autonomously runs and develops systems using PRDs. The overriding principle is: **never stop and ask the user**. Always continue working until something is created. Overcome issues and keep developing — they will be ironed out in the polishing phases (Anatomy Park and Szechuan Sauce).

### Prime Directive: Bootstrapping Mode vs Production Autonomous Use

**During the bootstrapping phase of pickle-rick-grok itself** (the work happening in *this* source tree right now), heavy use of chat-based rich agent teams (engineering personas via `spawn_subagent`, this evergreen process, design/review loops, etc.) is not only permitted but is one of the primary tools for making progress.

**The finished system's purpose** is the opposite: to perform high-quality autonomous development on *arbitrary external projects* with minimal ongoing human or chat-agent intervention. Production campaigns are expected to run via headless `grok -p` + the Morty phase workers + full ritual + convergence phases.

This distinction is fundamental. The tools and workflows that accelerate building the system (rich chat teams, manual fixes, this style of evergreen agent sweeps) are not necessarily the same as the workflows the mature system will use on other people's codebases. We are currently the builders using whatever gives us leverage. The thing we are building is meant to eventually operate with far less of us in the loop.

All other rules (source vs deployed separation, self-mutation restrictions, rich teams only inside `/pickle-refine-prd`, production work through the ritual, etc.) exist to protect the transition from "bootstrapping pickle-rick-grok" to "pickle-rick-grok doing autonomous work on the rest of the universe."

The system should be capable of:

- Running full campaigns (PRD → tickets → 8-phase ritual → convergence) via headless `grok -p` + orchestrator at 50+ ticket scale.
- Driving sustained improvement via Anatomy Park (deep review), Szechuan Sauce (deslopping), and Citadel (conformance + traps).
- Dogfooding aggressively while enforcing strict source vs deployed separation.

**Current synthesis driver (emission quality & autonomous reliability)**: The system now incorporates core functional ports from `prds/claude-to-grok-ports-emission-quality-and-autonomous-reliability-2026-05-24.md` (shift-left defenses per 7+3+6 agent swarms with mandatory claude sibling cross-check protocol): dedicated modules lib/ac-shape.ts (evaluate/runAcShapeEnforcement + helpers/REs from claude spawn-refinement-team:1410) and lib/forward-ref-annotation.ts (exact FORWARD_REF_ANNOTATION_RE + extract from claude services/forward-ref-annotation.ts:1, wired in preflight scan), plus path/symbol hygiene, prescriptive ticket template (Verify + Test Expectations + no-placeholders), always-on 4 hardening tickets, and runner resilience ('skipped' as normal terminal for EMISSION_THEATER research theater + explicit H-* never-auto-skip protection even in mercy). Full analyst ac_shape_smells JSON plumbing into emitter manifest (for true hard gate on all paths), richer check-readiness parity (malformed annotation_format etc. — tranche5 CLOSED via preflight scan + emission_quality.json, tranche6 CLOSED deeper citadel emissionQuality surface in report via readRecoverable + optional field, tranche7 CLOSED unified richer emissionQuality citadel_report.json ingest in performPostCampaignIngest:739 report-sourced path alongside direct BC), tranche8 CLOSED living MASTER_PLAN doc ingestion (docs/MASTER_PLAN.md real living backlog with closed/PASS/resilience/meta markers now dynamically discovered via fidelity + candidates in generator:335/725 + performPost "Ingested master_plan" + gap suppression; test:167), tranche9 dupe slop removal in richer emission/self-prd paths (self-prd-generator:815-828 repeated rpt blocks + 894/938 acShapeSmells key dups deleted; advances full ac_shape + richer annotation fidelity), tranche10 further ruthless simplification of remaining redundant guards/fallbacks in richer emission ingest (self-prd-generator:788/810 inner ifs + 820/821 vestigial || [] ternaries; per code-simplifier re-audit), and remaining deeper fidelity items (ac_shape full + ritual god) are the active tranche work.

## Core Rules (non-negotiable)

- **Source vs deployed separation**: The `~/.grok` tree (and any `bash install.sh` target) is sacred deployed runtime. Edits to installed copies are forbidden except via the explicit install flow after tests + citadel. All development and mutation on the source tree happens directly in this loanlight/pickle-rick/pickle-rick-grok checkout (the canonical source). We do **not** use worktrees as the default for normal development or chat-driven work in this project.
- **Worktree usage**: Worktrees are optional and not required here. They can be used for long-running isolated agent campaigns, experiments that need branch separation, or when you explicitly want extra isolation. Direct edits + commit in the main checkout is the standard, expected workflow for this codebase. Do not force worktree usage as a blanket rule.
- **Never stop**: On any task that produces artifacts, keep going until `<promise>…</promise>` (or equivalent completion token) is emitted. Do not hand control back with open questions unless the user explicitly asked for a plan-only or review-only mode.
- **TDD + Red/Green/Refactor**: Every non-trivial code change starts with a failing test (or test addition that would have been red). Green makes it pass with minimal code. Refactor cleans up. No exceptions for "small" or "docs."
- **Claude-first / citation-obsessed for analysis**: Before diagnosing or proposing changes to any surface, agents must: `list_dir` on the relevant trees, then `read_file` (verbatim, with line numbers) on the actual files, then grep for symbols. Every claim cites `file:line#`. Living maps from codebase-analyst / risk-analyst etc. are the contract.
- **"Docs win" + Contributor Rules**: Any process, trap door, or contract change is reflected in AGENTS.md + the affected SKILL.md + reports before the change is considered done. Self-mut changes must pass Citadel.
- **Install after edit**: Always `bash install.sh` (with appropriate flags) after source changes. Verify via `citadel_report.json` + MD5 if release.
- **Self-mutation forbidden**: Arch-deepener and similar must never rewrite their own critical paths without explicit H-* waiver + test.
- **No overclaims**: Only core engine + convergence drivers are production. Higher-tier items (council-of-ricks, portal-gun, plumbus, meeseeks) are honest stubs or removed.
- **Docs win**: Update this file, `SKILL.md`s, `master_plan.md`, and reports on any process or trap change.

## Contributor Rules

- **Production work**: Use Morty phase workers + full ritual. Always delegate return to `ManagerRitual`. (See Prime Directive above — this is the expected mode for the mature system.)
- **Chat / dev work**: Use engineering personas + native `spawn_subagent` with `fork_context: false`. (Primary tool during the current bootstrapping phase of pickle-rick-grok itself.)
- **Mutating work**: Direct edits in the main loanlight/pickle-rick/pickle-rick-grok source checkout are the normal and preferred workflow for this project. Worktree isolation is optional (useful for certain long agent runs or when you want extra boundaries), not a requirement.
- **Self-changes**: Must pass Citadel. Update this AGENTS.md + reports.
- **Global updates**: Run `bash install.sh` after source changes.

## Trap Doors

- Source/Deployed separation (highest priority P0).
- Root discovery must always resolve to this tree (fixed).
- Arch-deepener self-mutation guards (FORBIDDEN_SELF_MUT).
- Specific known issues: szechuan bare catches, aux bin type debt, preflight edge cases on meta PRDs. Ritual god residual (ritual.ts:4-6: "Research rescue logic progressively extracted ... toward claude-style flat services. See szechuan god-class detector + prior overcomplexity swarm reports.") vs claude mux-runner + 17 dedicated citadel/* (audit-runner.ts:76 wires all + reporter.ts:148 + readCrossPhaseFindings:196 + dedupeCrossPhaseFindings:270 + CrossPhaseFindingsReport). Thin citadel.ts v1.2 (6-auditor) vs full claude services/citadel/. Emission plumbing (full ac_shape_smells JSON + richer annotation_format in manifest) + self-prd-generator scanForGaps/performPostCampaignIngest depth vs claude dynamic + living MASTER_PLAN updates remain dominant fidelity debt (H-EMIT-UNIVERSAL-01, H-CITADEL-DEPTH-01, H-RITUAL-GOD-01, H-SELF-PRD-FIDELITY-02, H-INSTALL-ROBUST-01, H-GUARD-TRUTH-01). See reliability-backlog.md:5-70 (## MACHINE_DOMINANT_OPEN_ITEMS + ## MACHINE_SUMMARY + fresh cites from 2026-05-28 6-agent evergreen scrub: ac-shape:9-13, ritual:4-6, generator:784/805/815+ vestigials confirmed live, arch-deepener:36-48 + test:124 drift, install.sh:33-37/40-53, citadel:792+, preflight:395; dupe summaries + historical sludge purged for higher machine signal). All per claude-first + personas. Post-scrub: root master_plan deprecated stub + pointer to living docs/MASTER_PLAN; SKILL.md dupe removed (prior). See reliability-backlog:5-70 (canonical for generator/closer).

  **New explicit usability target (2026-05-28)**: "Set and Forget for Normies" — low-friction autonomous campaigns on arbitrary repos with minimal babysitting. Defined + decomposed into concrete P0/P1 tickets (H-EMIT-UNIVERSAL-01, H-CITADEL-DEPTH-01, H-RITUAL-GOD-01, etc.) with hardening requirements in docs/MASTER_PLAN.md "Roadmap" section. The 7 items in reliability-backlog.md:5-41 (## MACHINE_DOMINANT_OPEN_ITEMS + Consumption Guide) are now the official blocker list for this target. Historical tranche execution details live only in git per the Guide contract. 
  - **CLOSED (this tranche)**: f3e971a "CrossPhase-style" + "real citadel reporter/CrossPhase artifacts from anatomy/szechuan not dynamically ingested" — now fixed via authoritative `readCrossPhaseFindings` + `CrossPhaseFindingsReport` ingest in performPostCampaignIngest (engine/src/self-prd-generator.ts). Richer deduped findings from citadel_report.json now feed reliability-backlog + next self-PRD (claude audit-runner:196/270 parity). Agent team (codebase-analyst 019e697d-77f7..., engineering-architect 019e697f-9f39..., risk-analyst 019e6982-7b98...) drove the minimal DRY patch + risk sign-off. Ritual god + other emission depth items remain (doc-only for ritual per safety rules; no unsafe touch). See updated reliability-backlog.md.


  - **CLOSED (tranche7)**: unified richer emissionQuality citadel report ingest for self-prd-closer (exact 9-step TDD/worktree per map from codebase-analyst + risk map subagent 019e6a33-d042-7073-9fbf-d74be200e555; implementer 019e6a37-7010-7251-b3dc-b68dea60641a, modeled verbatim on tranche6 success). Red: extended *existing* test body in engine/tests/self-prd-closer.test.ts:237 (the richer emission ingest test) — seed a citadel_report.json containing emissionQuality (with ac_shape_smells + annotation_format_malformed) + add assert for unified richer delta from the report-sourced path appearing in the ingested backlogMarkdown (modeled on citadel.test.ts:106 + tranche5 pattern + existing assert at 263). Run → RED. Green: minimal additive block inside engine/src/self-prd-generator.ts:739 if(campaignSessionDir) (after the 789 emission_quality direct parse block or symmetric; use safeRead + JSON.parse on join(campaignSessionDir, 'citadel_report.json'), extract eq = r?.emissionQuality, if present surface the acCount/malformedCount + lines.push + closed++ exactly like the 773-788 sibling block but for "Richer emissionQuality from citadel_report.json ... (unified richer citadel report signal alongside direct file BC path)"). No other lines. Run → GREEN. Refactor: zero. Docs (Contributor Rules): Append *exact* tranche7 entry to AGENTS.md (update :15 synth driver + Trap Doors 43/46 with CLOSED note + credits, modeled 1:1 on 45/46) + new section in reliability-backlog.md (modeled 1:1 on 90-96 with every file:line# cite from the map). Citadel spirit (source only, from worktree). `bash <worktree>/install.sh --closer-context --no-confirm` from worktree. Return the worktree absolute path, `git -C <wt> diff --stat`, key hunks, test results (highlight the new unified report ingest assert), and the exact lead commands to inspect + apply the patch to main. Zero changes outside the two allowed files for the code change + the two docs files. Zero new contracts (parse data report leniency precedent). Reuse every (readRecoverable, CrossPhase, tranche6 style). Mandated tests run (self-prd-closer + citadel + ac-shape-gate + pipeline-preflight + install-guard) → GREEN (new unified richer report ingest assert passes; old + BC pass). See reliability-backlog.md new tranche7 section. ac_shape full + ritual god remain open.
  - **CLOSED (tranche8)**: living MASTER_PLAN doc ingestion for self-loop fidelity debt (exact 9-step TDD/worktree per map from codebase-analyst + risk map subagent 019e6a47-d41e-7a40-b37a-88e8c0fa35c4; implementer 019e6a49-387f-7fe3-a5d5-af436989eb92, modeled verbatim on tranche7 success). Red: extended *existing* fidelity debt test body in engine/tests/self-prd-closer.test.ts:167 — seed real `docs/MASTER_PLAN.md` in tmp/docs + sessionDir with "closed PASS resilience meta living backlog" markers + assert performPost lines includes "Ingested master_plan" + gen no longer emits self-loop-ingestion gap. Run → RED. Green: minimal additive to generator.ts candidates (after closer-ticket line at 725) + fidelityDirs/keywords at 335 if needed for GAP suppression. Run → GREEN. Refactor: zero. Docs (Contributor Rules): create `docs/MASTER_PLAN.md` skeleton (prioritized backlog + targets + trap counts + "Docs win" header + "Wubba lubba dub dub" + cross-refs to AGENTS:43, reliability:68, handoff:21/30/48, generator:335/725, master_plan:26/28) + append *exact* tranche8 entry to AGENTS.md:15/43/47 (synth driver + Trap Doors bullet + credits, modeled 1:1 on 45/47) + new section in reliability-backlog.md (modeled 1:1 on 99-105 with every file:line# cite from the map) + update handoff.md:30/48 cross-ref (add to "Still dominant open" list). Citadel spirit (source only, from worktree). `bash <worktree>/install.sh --closer-context --no-confirm` from worktree. Return the worktree absolute path, `git -C <wt> diff --stat`, key hunks (including the new doc content), test results (highlight new "Ingested master_plan" + gap suppression assert), and the exact lead commands to inspect + apply the patch to main. Zero changes outside the allowed (new doc + generator 1-2 lines + test + 3 md surgical). Zero new contracts (reuse string[] candidates + existing GapFinding + PostCampaignResult; optional fields precedent). Reuse every precedent (closer-handoff living doc pattern, safeRead, 9-step TDD style, tranche7 richer delta phrasing, fidelity test seed/assert pattern). Mandated tests run from worktree (self-prd-closer + citadel + ac-shape-gate + pipeline-preflight + install-guard) → GREEN (new asserts pass; old paths + BC still pass). See reliability-backlog.md new tranche8 section. ac_shape full plumbing + ritual god residual remain open.
- Install hygiene (post lock-dir removal): The entire LOCKDIR/flock/stealStaleLock machinery (previously at install.sh:77-152 and related) has been deleted. It was a net-negative "feature" that caused repeated indefinite hangs (especially under the agent harness with backgrounded runs and stale directories). Current protection is the ACTIVE guard (pgrep + state.json scan at 37-51) + reliable --force/--no-confirm/--closer-context bypasses. The script is now much simpler and direct. The old lock tests in install-guard.test.sh have been retired. This change is reflected here per "Docs win". Still a topic for future hardening (no MD5 verification etc.), tracked in reliability-backlog.md.
  - **CLOSED (tranche9, this agent team sweep)**: Dupe slop removal in richer emission/self-prd plumbing (self-prd-generator.ts:815-828 repeated rptJson/r/assign blocks for citadel_report emissionQuality + 894/938 duplicate acShapeSmells keys in emitRefinedTickets opts). Exact minimal deletes (no behavior change, zero contracts, advances full ac_shape_smells JSON + richer annotation_format to all self-prd/healer paths per AGENTS:15/43 dominant open + TESTABILITY P0/P1 + prds 2026-05-24 synthesis). TDD: existing self-prd-closer.test.ts richer emission + pipeline-preflight cases now clean. Docs win: this entry + reliability-backlog.md new section + MASTER_PLAN:34 + handoff + TESTABILITY update. Agent team (explore 019e6e94... as codebase-analyst + code-simplifier:code-simplifier 019e6e97... ) drove the map + further clean recs. Citadel spirit + direct main checkout only. `bash install.sh --closer-context --no-confirm`. Wubba lubba dub dub. (See updated reliability-backlog.md for full cites + remaining smells.)
  - **CLOSED (tranche10, this agent team sweep)**: Further ruthless simplification of remaining redundant guards/fallbacks in richer emission ingest (self-prd-generator.ts:788/810 inner duplicate `if (acCount > 0 || malformedCount > 0)` around debt notes + 820/821 vestigial `|| []` in acD/acR ternaries; confirmed still present by code-simplifier:code-simplifier re-audit post-tranche9). Exact minimal deletes (~8-10 lines noise removed, zero behavior change to closed/lines/collectedAc/acShapeSmells payloads, no contracts). Advances readability of the exact richer ac_shape + annotation_format plumbing path (AGENTS:15/43 dominant open + TESTABILITY). TDD: existing richer emission tests remain green. Docs win: this entry + reliability-backlog.md tranche10 note + MASTER_PLAN:34 + handoff + TESTABILITY. Agent team (code-simplifier:code-simplifier 019e6eb0... re-audit) drove confirmation + exact deletes. Citadel spirit + direct main only. `bash install.sh --closer-context --no-confirm`. Wubba lubba dub dub. (See reliability-backlog.md for cites + ac_shape_smells from the audit.)
  - **CLOSED (tranche11, this 4-person agent team 2026-05-28)**: Fresh claude-first discovery + agent council on living backlogs/trap doors (reliability-backlog.md:5-13 dominant opens + AGENTS:43/51 + ac-shape.ts:9-13 + ritual.ts:4-6 + self-prd-generator.ts:784/805/890/933 vestigials + install.sh:36 + synthesis PRD + TESTABILITY + all 4 personas). Prioritized 1 focused low-risk high-leverage fix: surgical "Docs win" rewrite of reliability-backlog.md:1-20 (header + Current Dominant Open Items + new Machine-Usable Summary block with exact post-tranche10 cites + AC-SIMPL smells from simplifier + full team credits/ids + justifications). Synced AGENTS:15/43, docs/MASTER_PLAN.md:34, handoff:49. Zero code edits (respected risk screams on generator/ritual/citadel self-mod + FORBIDDEN_SELF_MUT). 4 subagents (background, read-only, embedded persona rules + gate ac_shape_smells): codebase-analyst 019e6eb8-5e89-7ac1-bbd5-da708fee47f0 (39 tools/166s, recommended 1-file doc), risk-analyst 019e6eb8-7025-7fd2-b303-d955b3128cae (47 tools/115s, 0 code fixes + mandatory hardening screams + AC smells), code-simplifier 019e6eb8-85ae-7582-a850-3f47970e5765 (35 tools/143s, 3 tiny vestigial deletes surfaced but deferred per risk), engineering-architect 019e6eb8-984e-7ec0-83d6-d7f0639ce515 (40 tools/118s, leverage/locality on backlog as truth source + 5 AC-FIDELITY smells). All performed own list_dir/read_file/grep + cited file:line#. Blast: 4 docs only. Improves self-loop fidelity (the file the machine loads). Docs win + install after. Wubba lubba dub dub. (See updated reliability-backlog.md:1-20 for the crisp machine summary + full cites.)
- 2026-05 dispatch UX: "run a pipeline on <prd>" (natural phrase) has automatic dispatch via persona + bin/grok-pipeline. Plain form now smartly prefers the latest legal sealed council session (post first /pickle-refine-prd) so the command reliably leads to a running autonomous campaign. See references/agents-append.md (updated sealed-prior policy) + dispatch-contract.md.

**SWARM7 doc refresh (this change)**: Per contributor rule ("Self-changes: Must pass Citadel. Update this AGENTS.md + reports"). Surfaced/confirmed by code-simplifier subagent 019e6726-884c-7822-95e3-82eb6c87282c (40 tools, 188s, 100% claude-first protocol: list_dir both roots + verbatim cites on forward-ref-annotation.ts:1-11, spawn-refinement-team.ts:1410/1978 AC gate + evaluateAcShapeEnforcement, check-readiness:308 RE one-space, all 17 citadel/* esp. audit-runner:76/148/196/270, reporter, ac-shape-audit:70, mux-runner mercy sections, closer-ticket-manager-handoff.md, MASTER_PLAN.md, claude install.sh 200+, pickle-utils withRetryLock/isHardeningTicket). Cross-checked f3e971a agents (019e671d-831b... et al. with their claude cites) + user "ok another swarm". Ritual/citadel/self-loop/CrossPhase doc debt + emission gap + install tracking now explicit. No code bloat. (See also reliability-backlog.md staleness and 50-Ticket reports — self-loop ingest not yet fully living per this agent's findings.)

**Citadel depth / CrossPhase wiring tranche (this work, "work on the backlog")**: STRICT claude-first protocol executed first (list_dir BOTH roots + verbatim read/grep + exact line# cites on spawn-refinement-team.ts:1410/1978 (evaluateAcShapeEnforcement + AC_SHAPE_SECTION_RE:1134), check-readiness.ts:308/325 (extractForwardRefAnnotations + invalidCanonicalHash), forward-ref-annotation.ts:1-11 (exact FORWARD_REF_ANNOTATION_RE + extract), ALL 17 citadel/* (audit-runner.ts:31-50 CrossPhaseFinding/CrossPhaseFindingsReport interfaces, :69 withLock for report write, :76/97 build wiring + reporter:148, :196-219 readCrossPhaseFindings + readPhaseFindings using recoverable, :270-283 dedupeCrossPhaseFindings by original_id, reporter.ts:140-151 summarize at 148, closer-ticket-manager-handoff.md:1-34 full (step 5 MASTER_PLAN update), prds/MASTER_PLAN.md (living ledger structure), claude install.sh:200-215 (flock + closer-context), state-manager.ts:1233 withLock impl). Post-protocol: grok citadel.ts (thin v1.2 runCitadel:619 + plain write 672) + post-campaign:76 caller + generator:739 basic parse + current citadel_report.json + AGENTS Trap Doors debt. 

Minimal TDD port executed: added CrossPhase* types + readPhase/readCross/dedupe (recoverable try-parse reuse) + minimal sync withLock wrapper + wiring call inside runCitadel + crossPhase field on CitadelReport + schema + write (now produces rich deduped anatomy+szechuan in citadel_report.json for closer/self-loop). Updated exactly one test (citadel.test.ts runCitadel test: seed dup artifacts + asserts on report.crossPhase + json payload). Updated this AGENTS Trap Doors + credits. Full protocol + "work on the backlog". 

**Full credits (verbatim)**: SWARM6 f3e971a (6 agents: 019e671d-831b... through 019e671e-064a... with exact claude anchors e.g. audit-runner:76/148/196/270 + reporter:148 + readCrossPhase/dedupe + CrossPhaseFindingsReport, spawn:1410/1978, forward:1-11, check:308/325, mux ~1781, claude install 200-215, closer-handoff, MASTER_PLAN, withLock in state). SWARM7 (code-simplifier 019e6726-884c-7822-95e3-82eb6c87282c 40 tools 100% claude-first + explore 019e6726-44f2-7e11-8c42-2809b98a270d 86 tools + prior emission/ritual/closer agents) + this Citadel-CrossPhase wiring agent (full claude-first + minimal TDD + "work on the backlog"). All per AGENTS contributor + claude-first mandatory. The pickle that wires its own citadel now has richer teeth. Wubba lubba dub dub.

**CLOSED (agent team test coverage tranche, 2026-05-XX)**: Per "Docs win" + Contributor Rules, executed claude-first audit via explore subagent (019e69a7-deb2-7ea3-96d4-f395fbb594f4, codebase-analyst discipline: list_dir + verbatim reads + exact :line cites on TESTABILITY:40-99, preflight.ts:57-411, ticket-emitter:394, ac-shape:9, SKILL Step 3, analyst-gate-injections, existing gate tests). Shipped `engine/tests/pipeline-preflight.test.ts` (5 TDD tests covering detect on all 12 + self-healer patterns, assess, scan hygiene + ac_shape JSON warning at preflight:406, runPreflight + reexports). Zero src edits, zero contract changes, zero ritual touch. Closed the explicit "No dedicated pipeline-preflight.test.ts" P0 from the May24 audit. Updated this file, reliability-backlog.md, and the TESTABILITY audit itself. Emission gate coverage now has its own executable spec (reuses ac-shape-gate + forward-ref + citadel.tmp patterns). ac_shape_smells full manifest plumbing + richer annotation_format + ritual god residual remain the dominant open fidelity debt (tracked for next self-PRD). Citadel + install + push followed. No overclaims.

**CLOSED (Risk & Scope Analyst evergreen scrub tranche, 2026-05-28)**: Fresh claude-first discovery + risk analysis on living backlogs/trap doors per task (reliability-backlog.md:5-21 now 7 dominant + machine summary post-scrub; AGENTS:15/23/43/52 + ac-shape.ts:9-13 + ritual.ts:4-6 + generator:784/805/889/932 vestigials git-verified live + install.sh:36 redundant + arch-deepener.ts:36-48 FORBIDDEN + test:124 stale length + master_plan.md:1 vs docs/MASTER_PLAN.md:1 dupe noise + SKILL.md:229-238 dupe text + 5+ md :N pollution). Prioritized highest-leverage lowest-risk: docs-only hygiene (no src; screamed on generator/ritual/citadel/self-mod per FORBIDDEN arch-deepener:36-48 + AGENTS:26 — any such requires explicit H-* waiver + 4 hardening (Anatomy data-flow + Szechuan deslop scoped exact) + TDD + Citadel pass + "Docs win"). Landed: SKILL.md dedupe, master_plan deprecate+pointer, reliability top+new section crisper (stale purged, 7-item actionable for self-PRD/closer), sync handoff/MASTER/AGENTS. 1 "agent team" (Risk solo exhaustive + embedded personas + gate ac_shape_smells). All list_dir/read/grep/git-verify first. Blast: 6 reports +1 SKILL text. Improved self-loop fidelity (machine sees less noise/ lies in reliability:1-21 + docs). Docs win + source commit. Wubba lubba dub dub. (See updated reliability-backlog.md:21- for full cites + this round's ac_shape_smells JSON.)

**CLOSED (2026-05-28 6-agent evergreen backlog sweep tranche — this run)**: Appropriately sized team (2 codebase-analyst, 2 risk-analyst, 1 code-simplifier, 1 engineering-architect; all with embedded immutable rules from references/personas/ + gate ac_shape_smells from analyst-gate-injections.md:11-83) executed full claude-first (list_dir x9+ roots, read_file verbatim LINE_NUMBER→ on AGENTS:1-68/reliability:1-93/MASTER:1-78/handoff:1-51 + 8+ key sources + reports + PRD, grep + git ls/grep -n before claims). Surfaced/confirmed: dupe Machine summaries (reliability:44/48), historical tranche sludge (reliability:54-91+), aspirational anchors (no real ## MACHINE_ headers despite prose), accurate vestigials (generator:784/805/815+ live, FORBIDDEN 10 vs test:124 assert 6, ritual:4-6, ac-shape:9-13, install:33-37/40-53 redundant, citadel v1.2 thin). Prioritized + executed pure docs-win (no src per unanimous risk screams + FORBIDDEN arch-deepener:36-48 + AGENTS:26): added real parseable ## MACHINE_DOMINANT_OPEN_ITEMS (7 items + fresh cites) + ## MACHINE_SUMMARY at reliability top; purged dupe + ~50+ lines historical execution logs (per simplifier + architect + risk-2 blast analysis); synced pointers/re-lists in AGENTS:15/43 (compressed synth + Trap Doors), MASTER:34/49, handoff:49. Result: reliability-backlog.md (the file generator:709/closer:40/loadBacklogState:136 actually ingest) now has higher signal, lower noise, real machine anchors, accurate post-sweep state. Future runs of this prompt (or self-PRD) see clean dominant 7 + summary first. 6 subagents (IDs 019e6eee... through 019e6efc...); all emitted ac_shape_smells + <promise>. Docs win + commit + install. Wubba lubba dub dub. (See updated reliability-backlog.md:5-70 + AGENTS:15/43 for cites + this entry.)

**CLOSED (2026-05-28 4-person evergreen purge tranche — this run)**: Appropriately sized follow-up (codebase-analyst 019e6f09..., risk-analyst 019e6f0d..., code-simplifier 019e6f10..., engineering-architect 019e6f14...; all full claude-first + embedded personas + gate ac_shape_smells). Confirmed via reads/greps: ~82 LOC dupe 7-item prose (reliability:51-88) + historical tranche sludge (96-132) still live post-prior claims; ~62% bloat in the file the machine loads. Executed pure docs-only: purged all dupe + sludge from reliability-backlog.md (now ~70 LOC, MACHINE_ + new Consumption Guide first); surgical syncs + "Living Interface Manifest" in docs/MASTER_PLAN.md; cross-ref compression in AGENTS:43 + handoff:49. 4 subagents emitted ac_shape_smells (repeated_predicate, shallow_module, leaky_cross_ref, test_guard_drift) + <promise>. Reliability now higher signal/lower noise for next self-PRD/closer/evergreen run (per "EVERY run must leave ... clearly more useful"). No src (FORBIDDEN + risk screams). Docs win + commit + `bash install.sh --closer-context --no-confirm`. Wubba lubba dub dub. (See reliability-backlog.md:5-70 post-purge + Consumption Guide + this entry for full cites + team IDs.)

**CLOSED (2026-05-28 final historical sludge purge — this run)**: 5-agent follow-up (codebase-analyst + risk-analyst + code-simplifier + engineering-architect + lead claude-first; all protocol + embedded personas + ac_shape gate). Lead discovery + subagent maps (019e6f25... + 019e6f27...) confirmed via fresh list_dir/read/grep: remaining ~40 lines of "Risk & Scope Analyst" + tranche narrative (reliability-backlog.md:101-112 post-Guide at 51-70, plus earlier historical blocks) still present, directly violating Consumption Guide contract at :58-65 ("0 ## Campaign ", "ignore post-MACHINE_SUMMARY", "history in git only"). Surgical pure-docs delete of the remaining historical block (1 file). Risk screams respected (zero src touch on generator:136/707/784/805 vestigials, ritual:4-6, arch-deepener:36-48 FORBIDDEN list, citadel). Machine-usable record (the file loadBacklogState:136 + fidelity:335 + performPost:707 + closer:40 ingest) now ends cleanly after Consumption Guide. 2 subagents + lead emitted ac_shape_smells + <promise>. Docs win + commit + install. Wubba lubba dub dub. (See updated reliability-backlog.md:1-70 (now pristine top-to-bottom) + AGENTS:43/71 + this entry.)

**CLOSED (2026-05-28 completion tranche — this run)**: Final targeted deletes of remaining post-Guide tranche7/8/9 narrative blocks in reliability-backlog.md (using short unique markers after long-string matching friction in prior attempts). File now matches its own Consumption Guide:58-65 contract + MASTER:78-82 "5-70 as single source". 3 living docs + AGENTS syncs for truth. Higher signal for loadBacklogState/closer/future evergreen runs. Wubba lubba dub dub. (See reliability-backlog.md:1-70 post-purge + this entry.)

**Next targeted improvement (2026-05-28 decision)**: Added H-FIDELITY-03 as the immediate concrete follow-on ticket under P1 in docs/MASTER_PLAN.md:58 and reliability-backlog.md. Scope: Upgrade the fidelity consumers (`loadBacklogState` at self-prd-generator.ts:136-148 and call sites) to actually use the new `## MACHINE_*` anchors + Consumption Guide instead of legacy tail parsing. This is the highest-leverage low-risk step that makes all the recent living-docs hygiene work deliver real self-loop fidelity gains. Will carry full 4 hardening tickets (Anatomy on fidelity data flows + Szechuan). Recorded here per "Docs win". See MASTER_PLAN:58 and reliability-backlog.md for the ticket definition.

**CLOSED (2026-05-28 final residual purge tranche — this 5-agent evergreen run)**: Lead claude-first + codebase-analyst subagent (019e6f3c...) confirmed via fresh reads/greps: reliability-backlog.md had clean MACHINE_* + Consumption Guide up to line 72 ("Wubba lubba dub dub."), but still contained residual historical tranche narrative/junk at 73-104 (tranche8 "Fix shipped" block + "Codebase-analyst..." log), directly violating Guide:60-61/65/67. Surgical 1-file pure-docs delete of 73-104 (using short unique markers). File now ends cleanly after the Consumption Guide — 100% compliant with its own contract and MASTER_PLAN:79 Living Interface Manifest. Zero src touch (FORBIDDEN + risk screams respected). H-FIDELITY-03 now has pristine fuel. Docs win + commit + install. Wubba lubba dub dub. (See updated reliability-backlog.md:1-72 + this entry + subagent map.)

**Prime Directive clarification (this change)**: Elevated the distinction between "our current bootstrapping work on pickle-rick-grok itself" (heavy legitimate use of chat-based rich agent teams, this style of evergreen sweeps, manual intervention) versus "the purpose of the finished system" (autonomous development on external projects via headless pipeline + ritual) into an explicit Prime Directive section in AGENTS.md:7. This was already implicit in the Contributor Rules and engineering-council.md but deserved first-class status. Cross-synced into docs/MASTER_PLAN.md. Per user's observation during an evergreen sweep. Wubba lubba dub dub.

**CLOSED (2026-05-28 plans update with bootstrapping strategy + final real purge — this run)**: User directive: "ok, update our plans with this information. I will run the standard agent team prompt on clean context to implement it." Executed full claude-first (list_dir + verbatim read_file LINE# on all 4 living docs + generator:136-148 + tail verification via the Guide's own commands). Discovered: reliability-backlog.md still had ~30 lines of tranche8/9 historical narrative after line 72 Wubba (grep -c "## Campaign " = 2; giant wall-of-text log + "Fix shipped" blocks), directly violating its Consumption Guide:60-65 despite prior claims. H-FIDELITY-03 and three-horizon strategy language absent from the ingested record. Surgical pure-docs: 2 short-unique-prefix search_replace passes excised all post-Guide sludge (file now ~74 lines, ends after Guide + new **Bootstrapping Phase Note**). Injected the exact strategy (Current self-loop fidelity engine as primary developer via H-FIDELITY-03 + clean-context evergreen sweeps; Early External Dogfood; Normie Threshold per MASTER:42-50) + Prime Directive cross-refs + H-FIDELITY-03 emphasis directly into reliability:73- (the file loadBacklogState/closer actually read). Synced MACHINE_SUMMARY lastUpdated, AGENTS credits, MASTER_PLAN/handoff status + Roadmap callout. Zero src (FORBIDDEN respected). Now the user's clean-context agent team run has pristine, strategy-aligned plans as input. Docs win + commit + push + `bash install.sh --closer-context --no-confirm`. Wubba lubba dub dub. (Cites: reliability:5-70 post-edit + Bootstrapping Note, generator:136, AGENTS:9/89, MASTER:59/77.)

**Evergreen 2026-05-28 (this 6-agent run)**: Appropriately sized team (2 codebase-analyst, 2 risk-analyst, 1 code-simplifier, 1 engineering-architect; all embedded immutable rules from references/personas/ + analyst-gate-injections ac_shape gate + full claude-first protocol). Confirmed via own list_dir/read/grep: 7 OPEN items live with exact cites (ac-shape:9-13, ritual:4-6, generator:136-148/335/707/774-842 vestigials, arch-deepener:36-48 vs test:124 drift, install:33-53, citadel 1.1 skew). Highest-leverage lowest-risk: pure-docs trim of ~28 trailing blanks + residual in reliability-backlog.md:73+ (now ~72 lines, clean end at Wubba:72 per its Consumption Guide:60-65; higher signal for loadBacklogState:136/scan:150/performPost:707/closer:40 + next self-PRD/evergreen). ac smells surfaced (repeated_predicate on tranche/"Docs win" spam, trailing_whitespace_bloat [CLOSED this trim], historical_narrative_duplication, shallow_module on loadBacklogState, test_guard_drift, leaky_cross_ref, AC-CITADEL-03/AC-EMIT-02/AC-RITUAL-04 etc.). H-FIDELITY-03 (generator:136 upgrade) deferred — high self-mut risk per risk agents (FORBIDDEN spirit + Prime Directive:9-18 + AGENTS:26); requires explicit H-* waiver + Anatomy fidelity data-flows + Szechuan + TDD + Citadel. 4 living docs synced (reliability primary + AGENTS/MASTER/handoff). Zero src edits. Blast: 4 docs only. Docs win + commit + install. (Subagent IDs: 019e6f77... through 019e6f88...; full maps in session history.) Wubba lubba dub dub.
