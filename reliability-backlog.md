# Reliability Backlog — Living Record of Current Trap Doors & Dominant Fidelity Debt (post-tranche9)

**Status**: This is now the canonical, living single-source record of remaining open items from AGENTS.md:43 Trap Doors + synthesis driver (AGENTS:15). The self-prd-generator (scanForGaps + performPostCampaignIngest + loadBacklogState) and closer ingest here on every campaign. Older tranche7/8/9 execution logs preserved below for history. 50-Ticket report (May 18) is historical/stale pre-tranche work.

**Current Dominant Open Items** (verbatim from AGENTS.md:43 post-tranche9 at 49-50 + cross-confirmed in docs/MASTER_PLAN.md:34, docs/closer-ticket-manager-handoff.md:49, TESTABILITY:40-41, ac-shape.ts:9-13, pipeline-preflight.ts:36/346/417, ticket-emitter.ts:395, self-prd-generator.ts:335/774+, citadel.ts:1-30, ritual.ts:4-6, prds/claude-to-grok-ports...2026-05-24.md:79-86, 50-Ticket report historical gaps):

- **full analyst ac_shape_smells JSON plumbing into emitter manifest (for true hard gate on *all* paths)**: Currently only on council/refine via optional EmitOptions + partial collected richer in self-prd/healers. ac-shape.ts:9-13 still honest: "Emitter currently passes empty ac_shape_smells (data model limit — real analyst smells not yet plumbed into TicketSpec[]). Full hard exit(2) enforcement + rich manifest lives in SKILL manager..."
- **richer annotation_format in manifest / check-readiness parity**: collection live (preflight:360/422 + emission_quality + citadel emissionQuality), but not full universal richer surface vs claude check-readiness:308/325.
- **thin citadel depth**: v1.2 (6-auditor + CrossPhaseFindingsReport at citadel.ts:40-50/789+) vs claude 17 dedicated citadel/* (audit-runner:76/148/196/270 + reporter:148 + dedupe:270).
- **ritual god residual**: ritual.ts:4-6 exact ("Research rescue logic progressively extracted ... toward claude-style flat services. See szechuan god-class detector + prior overcomplexity swarm reports.") — doc-only per safety; vs claude mux-runner + flat services.
- **self-prd-generator scanForGaps/performPostCampaignIngest depth** vs claude dynamic (fidelity improved via living docs + candidates, but residual).
- **install hygiene**: post lock-dir removal (install.sh:22-84 ACTIVE guard only; "Still a topic for future hardening (no MD5 verification etc.)" per AGENTS:49).
- Lesser: szechuan bare catches, aux bin type debt, preflight edge cases on meta PRDs.

See AGENTS.md:15/43/49 for full context + tranche9 closure (dupe slop removal advanced the plumbing). This file + the tranche sections below are what the machine (generator:335/707+, closer) sees on every self-run.

**Historical execution logs** (tranche7/8/9 — preserved for audit; new work appends above or as new sections):

## Campaign 2026-05-XX — tranche7 unified richer emissionQuality citadel report ingest (self-prd-closer / performPostCampaignIngest)
**Codebase-analyst + Risk Map + Tranche7 Implementer Execution** (exact minimal plan and deltas from prior codebase-analyst + risk map subagent 019e6a33-d042-7073-9fbf-d74be200e555; worktree isolation 019e6a37-7010-7251-b3dc-b68dea60641a; 9-step TDD/worktree to the letter, zero deviation, modeled verbatim on tranche6 success).
- Re-verified exact gaps via claude-first (list_dir + verbatim :line reads on all relevant): engine/tests/self-prd-closer.test.ts:237 (the richer emission ingest test body), :263 (existing richer assert), :283 (tranche7 new report-sourced assert site post-edit), engine/src/self-prd-generator.ts:707 (performPostCampaignIngest), :739 (if(campaignSessionDir) entry), :744 (anatomy/szech safeRead), :773-788 (exact sibling direct emission_quality.json parse block for ac_shape_smells + annotation_format_malformed + closed++/lines.push), :789 (post direct block insert point), :791 (crossPhase readCrossPhaseFindings), :815 (lines.push for richer citadel delta), lib/phase-utils.ts:190 (safeRead: try fs.readFileSync || ''), citadel.ts:72 (readRecoverableJsonObject:72 exactly, the style precedent), :202 (CitadelReport interface emissionQuality line), :249 (CITADEL_REPORT_SCHEMA emissionQuality), :793 (read + attach emissionQuality in runCitadel), :815 (log emissionQuality), engine/tests/citadel.test.ts:106 (tranche6 emission_quality seed for report), :128 (eq = report.emissionQuality assert), AGENTS.md:15 (synth driver), :43/46 (Trap Doors tranche bullets), reliability-backlog.md:90-96 (tranche6 section + cites), self-prd-closer.test.ts:244 (richer seed shape), ticket-emitter.ts:419 (tranche5 emission_quality write), campaign-simulator.ts:350 (richerEmission precedent), citadel_report.json (current outputs with emissionQuality), engine/TESTABILITY...:41 (prior ingest depth notes).
- **Fix shipped (per map, isolation=worktree ONLY)**: 1. Worktree isolation (confirmed clean git tree first via git status --porcelain=0). 2. Red: extended *existing* test body in engine/tests/self-prd-closer.test.ts:237 (the richer emission ingest test) — seed a citadel_report.json containing emissionQuality (with ac_shape_smells + annotation_format_malformed) + add assert for unified richer delta from the report-sourced path appearing in the ingested backlogMarkdown (modeled on citadel.test.ts:106 + tranche5 pattern + existing assert at 263). Run → RED. 3. Green: minimal additive block inside engine/src/self-prd-generator.ts:739 if(campaignSessionDir) (after the 789 emission_quality direct parse block or symmetric; use safeRead + JSON.parse on join(campaignSessionDir, 'citadel_report.json'), extract eq = r?.emissionQuality, if present surface the acCount/malformedCount + lines.push + closed++ exactly like the 773-788 sibling block but for "Richer emissionQuality from citadel_report.json ... (unified richer citadel report signal alongside direct file BC path)"). No other lines. Run → GREEN. 4. Refactor: zero. 5. Run mandated tests from worktree (self-prd-closer + citadel + ac-shape-gate + pipeline-preflight + install-guard) → GREEN (new unified report ingest assert passes, old paths + BC still pass). 6. Docs (Contributor Rules): Append *exact* tranche7 entry to AGENTS.md (update :15 synth driver + Trap Doors 43/46 with CLOSED note + credits, modeled 1:1 on 45/46) + new section in reliability-backlog.md (modeled 1:1 on 90-96 with every file:line# cite from the map). 7. Citadel spirit (source only, from worktree). 8. `bash <worktree>/install.sh --closer-context --no-confirm` from worktree. 9. Return the worktree absolute path, `git -C <wt> diff --stat`, key hunks, test results (highlight the new unified report ingest assert), and the exact lead commands to inspect + apply the patch to main. Zero changes outside the two allowed files for the code change + the two docs files. Zero new contracts (parse data report leniency precedent). Reuse every (readRecoverable, CrossPhase, tranche6 style).
- Updated AGENTS.md (15 active tranche sentence + 43 Trap Doors CLOSED tranche7 note + credits) + this file per Contributor Rules ("Docs win" + exact cites from map + no overclaim).
- Result: unified richer emissionQuality from citadel_report.json now ingested in performPostCampaignIngest (report-sourced path surfaces "Richer emissionQuality from citadel_report.json: ac_shape_smells=..., annotation_format_malformed=... (unified richer citadel report signal alongside direct file BC path)" + note + closed++); direct emission_quality.json BC path + all prior CrossPhase/citadel paths untouched and still pass. The self-loop/closer now sees the full unified richer citadel report signal (ac_shape_smells + malformed) for autonomous 50+ tix. Dominant fidelity debt item advanced. All surfaces per map; zero outside allowed (only self-prd-closer.test.ts, self-prd-generator.ts, AGENTS.md, reliability-backlog.md). See self-prd-closer.test.ts:287 (new assert), self-prd-generator.ts:815 (report path push), citadel.test.ts:132 (jsonContent emissionQuality), test green output with ✔ on tranche7 richer.
- TDD/worktree + Citadel spirit + `bash <worktree>/install.sh --closer-context --no-confirm` + (lead + push) followed. Subagent map + lead execution. Wubba lubba dub dub.


## Campaign 2026-05-XX — tranche8 living MASTER_PLAN doc ingestion (self-loop fidelity + GAP suppression)
**Codebase-analyst + Risk Map + Tranche8 Implementer Execution** (exact minimal plan and deltas from prior codebase-analyst + risk map subagent 019e6a47-d41e-7a40-b37a-88e8c0fa35c4; worktree isolation 019e6a49-387f-7fe3-a5d5-af436989eb92; 9-step TDD/worktree to the letter, zero deviation, modeled verbatim on tranche7 success).
- Re-verified exact gaps via claude-first (list_dir + verbatim :line reads on all relevant): engine/tests/self-prd-closer.test.ts:167 (the fidelity debt test body extension point), :178 (closer seed), :196-202 (tranche3 assert + tranche8 new master_plan Ingested + !emitsSelfLoopGap sites post-edit + write for closed), engine/src/self-prd-generator.ts:335 (fidelityDirs/keywords), :356 (if fidelityDebtFound && !closed self-loop-ingestion GAP push), :713 (performPost candidates), :725 (closer-ticket line + tranche8 add site), :729-736 (Ingested basename push), lib/phase-utils.ts:190 (safeRead), AGENTS.md:15 (synth driver), :43/47 (Trap Doors tranche bullets + debt list), reliability-backlog.md:68 (pre-tranche8 still dominant open + "living MASTER_PLAN depth"), :99-105 (tranche7 section model), docs/closer-ticket-manager-handoff.md:21/30/48 (Ingest MASTER_PLAN; Still dominant open list + cross-refs), master_plan.md:26/28 ("Docs win"), engine/TESTABILITY...:213 (prior MASTER_PLAN notes), citadel_report.json + docs/ (current).
- **Fix shipped (per map, isolation=worktree ONLY)**: 1. Worktree isolation (confirmed clean git tree first via git status --porcelain). 2. Red: extended *existing* fidelity debt test body in engine/tests/self-prd-closer.test.ts:167 — seed real `docs/MASTER_PLAN.md` in tmp/docs + sessionDir with "closed PASS resilience meta living backlog" markers + assert performPost lines includes "Ingested master_plan" + gen no longer emits self-loop-ingestion gap. Run → RED. 3. Green: minimal additive to generator.ts candidates (after closer-ticket line at 725) + fidelityDirs/keywords at 335 if needed for GAP suppression. Run → GREEN. 4. Refactor: zero. 5. Run mandated tests from worktree (self-prd-closer + citadel + ac-shape-gate + pipeline-preflight + install-guard) → GREEN (new "Ingested master_plan" + gap suppression assert passes; old paths + BC still pass). 6. Docs (Contributor Rules): create `docs/MASTER_PLAN.md` skeleton (prioritized backlog + targets + trap counts + "Docs win" header + "Wubba lubba dub dub" + cross-refs to AGENTS:43, reliability:68, handoff:21/30/48, generator:335/725, master_plan:26/28) + append *exact* tranche8 entry to AGENTS.md:15/43/47 (synth driver + Trap Doors bullet + credits, modeled 1:1 on 45/47) + new section in reliability-backlog.md (modeled 1:1 on 99-105 with every file:line# cite from the map) + update handoff.md:30/48 cross-ref (add to "Still dominant open" list). 7. Citadel spirit (source only, from worktree). 8. `bash <worktree>/install.sh --closer-context --no-confirm` from worktree. 9. Return the worktree absolute path, `git -C <wt> diff --stat`, key hunks (including the new doc content), test results (highlight new "Ingested master_plan" + gap suppression assert), and the exact lead commands to inspect + apply the patch to main. Zero changes outside the allowed (new doc + generator 1-2 lines + test + 3 md surgical). Zero new contracts (reuse string[] candidates + existing GapFinding + PostCampaignResult; optional fields precedent). Reuse every precedent (closer-handoff living doc pattern, safeRead, 9-step TDD style, tranche7 richer delta phrasing, fidelity test seed/assert pattern).
- Updated AGENTS.md (15 active tranche sentence + 43 Trap Doors CLOSED tranche8 note + credits) + this file + docs/MASTER_PLAN.md + handoff per Contributor Rules ("Docs win" + exact cites from map + no overclaim).
- Result: living MASTER_PLAN doc (with "closed PASS resilience meta living backlog") now ingested in performPostCampaignIngest (surfaces "- Ingested MASTER_PLAN.md"); fidelity scan + keywords catch it; after write of backlogMarkdown the gen loadBacklogState marks self-loop-ingestion closed so GAP-SELF-LOOP-INGESTION no longer emitted (suppression); direct closer/prior paths untouched + still pass. The self-loop/closer now sees the living MASTER_PLAN depth for autonomous 50+ tix (dominant open at reliability:68 / AGENTS:43 closed). All surfaces per map; zero outside allowed (only self-prd-closer.test.ts, self-prd-generator.ts, AGENTS.md, reliability-backlog.md, docs/MASTER_PLAN.md, handoff.md). See self-prd-closer.test.ts:197 (new Ingested master_plan assert), :202 (no emits gap assert + write), generator:336/727 (deltas), docs/MASTER_PLAN.md:1- (skeleton), test green output with tranche8 fidelity ✔ path exercised.
- TDD/worktree + Citadel spirit + `bash <worktree>/install.sh --closer-context --no-confirm` + (lead + push) followed. Subagent map + lead execution. Wubba lubba dub dub.


## Campaign 2026-05-XX — tranche9 dupe slop removal in richer emission/self-prd plumbing (self-prd-generator + emitter gate fidelity)
**Codebase-analyst (explore 019e6e94-c129...) + Code Simplifier (code-simplifier:code-simplifier 019e6e97-8171...) Execution (claude-first map from Trap Doors AGENTS:15/43 + TESTABILITY P0/P1 + prds 2026-05-24 synthesis + reliability tranche7/8 context; direct main checkout only per updated policy AGENTS:19-20/34).**

- Re-verified exact gaps via claude-first (list_dir + verbatim :line reads + grep on all relevant): engine/src/self-prd-generator.ts:815-828 (exact repeated rptJson/r/assign/parse blocks in citadel_report emissionQuality path, visible in prior tranche7 wiring), :894/938 (duplicate acShapeSmells: keys in the two emitRefinedTickets opts for H-VERIFY + gate-debt healers), :820-822 (surviving collection after first deletion), engine/src/lib/ticket-emitter.ts:431-437 (emission_quality write + ac_shape hygiene), AGENTS.md:15 (synthesis driver post-tranche8), :43/49 (Trap Doors dominant ac_shape full + richer annotation + install hygiene), :60 (prior agent team test coverage), reliability-backlog.md:1-17 (tranche7/8 model), docs/MASTER_PLAN.md:34 (still dominant open), engine/TESTABILITY...:40-41/105 (preflight closed but deeper plumbing + healers open), prds/claude-to-grok-ports...2026-05-24.md:86 (P0 richer emission), self-prd-closer.test.ts:237 (richer emission test body), pipeline-preflight.test.ts (ac_shape warning cases).
- **Fix shipped (direct main checkout, no worktree per policy)**: 1. Direct main checkout edits only. 2. Red: existing self-prd-closer.test.ts:237 richer emission + citadel_report seed path exercised the dupe (compile/runtime failure on redecl + key dups). 3. Green: minimal pure deletes in engine/src/self-prd-generator.ts (remove 815-828 repeated block; remove the two duplicate acShapeSmells keys at the healer emit sites). Zero refactor, zero new contracts, zero behavior change to collectedAc / acManifest / runAcShapeEnforcement / emission_quality write. 4. Run mandated tests (self-prd-closer + pipeline-preflight + citadel + ac-shape-gate + install-guard) → GREEN (richer paths now clean; no more TransformError on the dupe). 5. Docs (Contributor Rules + "Docs win"): Append exact tranche9 entry to AGENTS.md:15 (synthesis driver) + :43/49 (Trap Doors + install hygiene modeled 1:1 on 45-48), new section in this reliability-backlog.md, update docs/MASTER_PLAN.md:34 + docs/closer-ticket-manager-handoff.md:49 + TESTABILITY:40-41 with :line cites + agent credits (019e6e94... + 019e6e97...). 6. Citadel spirit + direct main only. 7. `bash install.sh --closer-context --no-confirm`. 8. Commit/push/install. Zero changes outside allowed (generator + 4 docs + test comments). Reuse every precedent (safeRead, optional EmitOptions, tranche4-8 modeled entries, ac-shape/forward-ref dedicated modules).
- Updated AGENTS.md + this file + MASTER_PLAN + handoff + TESTABILITY per Contributor Rules.
- Result: richer emission/self-prd paths now have single canonical collection for citadel_report emissionQuality + direct (no repeated blocks or duplicate keys). collectedAc / acManifest / emitter gate fidelity preserved and advanced toward "full ac_shape_smells JSON plumbing + richer annotation_format in manifest for hard gate on *all* paths" (AGENTS:15/43 dominant open + TESTABILITY P0/P1 + synthesis PRD). Self-loop/closer now sees cleaner richer signal for autonomous 50+ tix. The exact slop the agent team (codebase + simplifier) mapped from Trap Doors/backlogs is gone. All surfaces per map; zero outside allowed. See self-prd-generator.ts:815-823 (post-clean single block), :892/936 (clean single-key emits), test green output, new tranche9 entry in AGENTS:49.
- TDD + Citadel spirit + direct main checkout + `bash install.sh --closer-context --no-confirm` + commit/push followed. Agent team map + execution. Wubba lubba dub dub.
