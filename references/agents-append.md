# === Pickle Rick (Grok) ===

Pickle Rick is installed.

Useful commands:
- /pickle-rick
- /pickle-pipeline
- /microverse
- /anatomy-park
- /szechuan-sauce
- /citadel
- /help-pickle

When spawning subagents, prefer the installed personas:
- "pickle-rick" (manager)
- "morty-phase-researcher", "morty-phase-implementer", etc.

Run `bash ~/.grok/pickle-rick-grok/uninstall.sh` (or wherever you installed from) to remove.

# === End Pickle Rick ===